# Payment microservice

Usar versión de node:

```terminal
nvm use
```

Instalar dependencias:

```terminal
nvm use
npm install
```

Para levantar el servidor:

```terminal
docker-compose up -d
```
